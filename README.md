**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install` 
2. Run the app:
   `npm run dev`

## About "Finding - Job Matching"
This program is a job matching names "Finding", a platform where both people who seek for careers that match their qualifications, and people who seek for candidates who match their needs directly.

Let's see how this program works.
